async function validationCaracters(num) {
        console.log('NNUUUUUMMMMM', num)
        console.log('typeof', typeof num)
        let number = parseFloat(num);

        if (!isNaN(number)) {
        // if (typeof number == 'number') {
                return true
        } 
                return false
        
};

module.exports = validationCaracters;
